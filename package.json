{
  "name": "websocket-test",
  "version": "1.0.0",
  "description": "Test script for STOMP over WebSocket",
  "main": "test-ws.js",
  "scripts": {
    "test": "node test-ws.js"
  },
  "dependencies": {
    "@stomp/stompjs": "^7.0.0",
    "ws": "^8.16.0"
  }
}
